---
title: "Form controls"
weight: 2
---

The interactive inputs: `Button`, `Input`, `Checkbox`/`Toggle`, `Select`, and the
single-choice `Segmented` strip. Each follows the same
[controlled/uncontrolled](/salle/concepts/use-controllable/) model and takes its look from
the active [skin](/salle/concepts/skins/).
